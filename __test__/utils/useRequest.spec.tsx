/**
 * @jest-environment jsdom
 */

import React, { Suspense } from 'react';
import { render, screen } from '@testing-library/react';

import { RecoilRoot } from 'recoil';

describe('utils/useRequest 테스트', () => {
	it('fetcher 함수 테스트', () => {});
});
